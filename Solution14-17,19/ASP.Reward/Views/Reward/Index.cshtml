@using ASP.Reward.Models;

@model List<Award>

    @{
    ViewBag.Title = "Index";
    }

    <h2>Награды</h2>
    @Html.ActionLink("Add", "Add", "Award", new { }, new { @class = "btn btn-primary" })

    <table class="table table-bordered" style="margin-top: 50px;">
        <thead>
            <tr>
                <th>Название</th>
                <th>Описание</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var award in Model)
            {
            <tr>
                <td>@award.Title</td>
                <td>@award.Description</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "Award", new { AwardId = award.Id }, new { @class = "btn btn-default" })
                    @Html.ActionLink("Delete", "Delete", "Award", new { AwardId = award.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
            }
    </tbody>
</table>
